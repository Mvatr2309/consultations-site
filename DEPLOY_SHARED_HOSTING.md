# Деплой на обычный веб-хостинг (без VPS)

## Важно: Ограничения обычного хостинга

**Проблема:** Большинство обычных веб-хостингов (shared hosting) **НЕ поддерживают** FastAPI напрямую, потому что:

1. FastAPI требует запуска долгоживущего процесса (сервер должен работать постоянно)
2. Обычный хостинг обычно поддерживает только PHP, статические файлы или Python через CGI (который не подходит для FastAPI)
3. Нужен доступ к командной строке и возможность устанавливать пакеты через pip

## Решения: Где можно задеплоить

### ✅ Вариант 1: PythonAnywhere (рекомендуется для начинающих)

**Плюсы:**
- Бесплатный тариф для начала
- Простой интерфейс
- Поддержка Python и веб-приложений
- Не требует знания командной строки

**Минусы:**
- Ограничения на бесплатном тарифе
- Нужна небольшая адаптация кода

**Инструкция:**

1. **Регистрация:**
   - Откройте https://www.pythonanywhere.com/
   - Зарегистрируйтесь (бесплатный аккаунт)

2. **Загрузка файлов:**
   - В панели управления найдите **"Files"**
   - Создайте папку `consultations`
   - Загрузите все файлы проекта через веб-интерфейс

3. **Настройка базы данных:**
   - В разделе **"Databases"** создайте SQLite базу
   - Или используйте встроенную SQLite

4. **Создание веб-приложения:**
   - Перейдите в **"Web"** → **"Add a new web app"**
   - Выберите **"Manual configuration"** → **"Python 3.10"**
   - В разделе **"Code"** укажите путь к файлу: `/home/ваш-логин/consultations/app/main.py`
   - В разделе **"WSGI configuration file"** откройте файл и замените содержимое на:

```python
import sys
path = '/home/ваш-логин/consultations'
if path not in sys.path:
    sys.path.append(path)

from app.main import app
application = app
```

5. **Настройка переменных окружения:**
   - В разделе **"Web"** → **"Environment variables"**
   - Добавьте:
     - `DATABASE_URL=sqlite:///./data/consultations.db`
     - `ADMIN_TOKEN=ваш-пароль`
     - `EXPERT_TOKEN=ваш-пароль`

6. **Установка зависимостей:**
   - Откройте **"Consoles"** → **"Bash"**
   - Выполните:
   ```bash
   cd consultations
   pip3.10 install --user -r requirements.txt
   ```

7. **Перезапуск:**
   - В разделе **"Web"** нажмите **"Reload"**

**Важно:** На PythonAnywhere нужно использовать `application` вместо `app` в WSGI файле.

---

### ✅ Вариант 2: Railway.app (простой и современный)

**Плюсы:**
- Очень простой деплой
- Бесплатный тариф
- Автоматический деплой из Git
- Поддержка FastAPI из коробки

**Инструкция:**

1. **Регистрация:**
   - Откройте https://railway.app/
   - Войдите через GitHub

2. **Создание проекта:**
   - Нажмите **"New Project"**
   - Выберите **"Deploy from GitHub repo"**
   - Выберите ваш репозиторий

3. **Настройка:**
   - Railway автоматически определит Python проект
   - Добавьте переменные окружения в разделе **"Variables"**:
     - `DATABASE_URL=sqlite:///./data/consultations.db`
     - `ADMIN_TOKEN=ваш-пароль`
     - `EXPERT_TOKEN=ваш-пароль`

4. **Деплой:**
   - Railway автоматически задеплоит проект
   - Вы получите URL вида `ваш-проект.railway.app`

**Создайте файл `Procfile` в корне проекта:**
```
web: uvicorn app.main:app --host 0.0.0.0 --port $PORT
```

---

### ✅ Вариант 3: Render.com

**Плюсы:**
- Бесплатный тариф
- Простой деплой
- Автоматический деплой из Git

**Инструкция:**

1. **Регистрация:**
   - Откройте https://render.com/
   - Войдите через GitHub

2. **Создание Web Service:**
   - Нажмите **"New"** → **"Web Service"**
   - Подключите ваш GitHub репозиторий

3. **Настройка:**
   - **Name:** consultations
   - **Environment:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

4. **Переменные окружения:**
   - Добавьте в разделе **"Environment"**:
     - `DATABASE_URL=sqlite:///./data/consultations.db`
     - `ADMIN_TOKEN=ваш-пароль`
     - `EXPERT_TOKEN=ваш-пароль`

5. **Деплой:**
   - Нажмите **"Create Web Service"**
   - Render автоматически задеплоит проект

---

### ✅ Вариант 4: Beget (российский хостинг с поддержкой Python)

**Плюсы:**
- Российский хостинг
- Поддержка Python
- Низкая цена

**Важно:** Нужно проверить, поддерживает ли ваш тариф Python и SSH доступ.

**Инструкция:**

1. **Проверка поддержки Python:**
   - Войдите в панель управления Beget
   - Проверьте, есть ли раздел **"Python"** или **"SSH доступ"**

2. **Если есть SSH:**
   - Следуйте инструкции из `DEPLOY.md` (как для VPS)

3. **Если только через панель:**
   - Загрузите файлы через FTP
   - Настройте Python приложение через панель (если доступно)

---

### ✅ Вариант 5: Timeweb (обычный хостинг)

**Плюсы:**
- Российский хостинг
- Некоторые тарифы поддерживают Python

**Инструкция:**

1. **Проверка тарифа:**
   - Убедитесь, что ваш тариф поддерживает Python
   - Обычно это тарифы "Бизнес" и выше

2. **Если есть SSH:**
   - Используйте инструкцию из `DEPLOY.md`

3. **Если нет SSH:**
   - Свяжитесь с поддержкой Timeweb
   - Уточните, можно ли запустить FastAPI на вашем тарифе

---

## Сравнение вариантов

| Хостинг | Сложность | Цена | Подходит для FastAPI |
|---------|-----------|------|---------------------|
| **Railway.app** | ⭐ Очень просто | Бесплатно/от $5 | ✅ Да |
| **Render.com** | ⭐ Очень просто | Бесплатно/от $7 | ✅ Да |
| **PythonAnywhere** | ⭐⭐ Просто | Бесплатно/от $5 | ⚠️ С адаптацией |
| **Beget** | ⭐⭐⭐ Средне | От 200₽/мес | ⚠️ Зависит от тарифа |
| **Timeweb** | ⭐⭐⭐ Средне | От 300₽/мес | ⚠️ Зависит от тарифа |
| **VPS (Yandex Cloud)** | ⭐⭐⭐⭐ Сложно | От 1000₽/мес | ✅ Да |

---

## Рекомендация

**Для начинающих:** Используйте **Railway.app** или **Render.com** - это самые простые варианты:
- Не нужно настраивать сервер
- Автоматический деплой из Git
- Бесплатный тариф для начала
- Полная поддержка FastAPI

**Для российского хостинга:** Если нужен именно российский хостинг, лучше использовать **VPS** (виртуальный сервер), так как обычный shared hosting редко поддерживает FastAPI.

---

## Адаптация кода для PythonAnywhere

Если используете PythonAnywhere, нужно создать файл `passenger_wsgi.py` в корне проекта:

```python
import sys
import os

# Добавляем путь к проекту
sys.path.insert(0, os.path.dirname(__file__))

# Импортируем приложение
from app.main import app

# PythonAnywhere требует переменную application
application = app
```

---

## Что делать, если хостинг не поддерживает FastAPI?

Если ваш хостинг не поддерживает FastAPI, у вас есть варианты:

1. **Перейти на VPS** (виртуальный сервер) - больше контроля, но сложнее настройка
2. **Использовать Railway/Render** - проще, но зарубежные сервисы
3. **Переписать на PHP/Node.js** - если хостинг поддерживает только эти технологии (не рекомендуется, много работы)

---

## Быстрый старт с Railway (самый простой вариант)

1. Зарегистрируйтесь на https://railway.app/
2. Подключите GitHub репозиторий
3. Railway автоматически определит Python проект
4. Добавьте переменные окружения
5. Готово! Сайт будет доступен через 2-3 минуты

**Это самый простой способ для новичков!** 🚀

