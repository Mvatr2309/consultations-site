# Супер подробная инструкция по деплою на Timeweb для новичков

Эта инструкция написана специально для тех, кто не умеет программировать. Каждый шаг объяснен максимально подробно.

---

## ⚠️ ВАЖНО: Что вам понадобится

1. **Аккаунт Timeweb** с тарифом **VPS** (виртуальный сервер)
   - Обычный shared hosting НЕ подойдет!
   - Нужен именно VPS/VDS тариф
   - Минимальный тариф обычно стоит от 200-300 рублей/месяц

2. **Доступ к вашему компьютеру** для загрузки файлов

3. **30-60 минут времени** на настройку

---

## Часть 1: Подготовка - что такое VPS и зачем он нужен

**Простыми словами:**
- VPS (Virtual Private Server) - это виртуальный компьютер в интернете
- На нем можно установить любые программы, как на обычном компьютере
- Ваш сайт будет работать 24/7, даже когда ваш компьютер выключен

**Почему нужен VPS:**
- FastAPI (ваш сайт) требует постоянно работающий сервер
- Обычный хостинг этого не поддерживает
- VPS дает полный контроль над сервером

---

## Часть 2: Заказ VPS на Timeweb

### Шаг 1: Вход в панель Timeweb

1. Откройте сайт: https://timeweb.com/
2. Нажмите кнопку **"Войти"** в правом верхнем углу
3. Введите ваш логин и пароль
4. Нажмите **"Войти"**

### Шаг 2: Заказ VPS

1. В панели управления найдите раздел **"VPS"** или **"Виртуальные серверы"**
2. Нажмите **"Заказать VPS"** или **"Создать VPS"**

### Шаг 3: Выбор тарифа

**Рекомендуемый тариф для начала:**
- **CPU:** 2 ядра
- **RAM:** 2-4 ГБ
- **Диск:** 20-40 ГБ SSD
- **ОС:** Ubuntu 22.04 LTS (это важно!)

**Как выбрать:**
- Выберите самый дешевый тариф с Ubuntu
- Обычно это тариф "Стартовый" или "Базовый"
- Цена: примерно 200-400 рублей/месяц

### Шаг 4: Настройка VPS

Заполните форму:

1. **Имя сервера:** `consultations-server` (или любое другое)
2. **Операционная система:** выберите **Ubuntu 22.04 LTS**
3. **Пароль root:** придумайте сложный пароль (запишите его!)
   - Минимум 8 символов
   - Используйте буквы, цифры и символы
   - Пример: `MyP@ssw0rd2024!`

4. Нажмите **"Создать"** или **"Заказать"**

### Шаг 5: Ожидание создания

- Создание VPS займет 5-15 минут
- Вы получите уведомление, когда сервер будет готов
- **ВАЖНО:** Запишите данные для доступа:
  - **IP-адрес сервера** (например: `185.123.45.67`)
  - **Пароль root** (который вы указали)

---

## Часть 3: Подключение к серверу

### Что такое SSH и зачем он нужен

**SSH** - это способ подключиться к серверу удаленно, как будто вы сидите за его клавиатурой.

### Шаг 6: Подключение через Windows

**Если у вас Windows:**

#### Вариант A: Использование встроенного терминала (Windows 10/11)

1. Нажмите клавишу **Win** (кнопка с логотипом Windows)
2. Введите **"PowerShell"** или **"Терминал"**
3. Откройте приложение

4. Введите команду (замените `185.123.45.67` на ваш IP):
   ```bash
   ssh root@185.123.45.67
   ```

5. Нажмите **Enter**

6. При первом подключении появится вопрос:
   ```
   The authenticity of host '185.123.45.67' can't be established.
   Are you sure you want to continue connecting (yes/no)?
   ```
   - Введите **`yes`** и нажмите Enter

7. Введите пароль root (который вы указали при создании)
   - **Важно:** При вводе пароля символы не отображаются - это нормально!
   - Просто введите пароль и нажмите Enter

8. Если все правильно, вы увидите:
   ```
   root@consultations-server:~#
   ```
   Это означает, что вы подключились! 🎉

#### Вариант B: Использование PuTTY (альтернатива)

1. Скачайте PuTTY: https://www.putty.org/
2. Установите программу
3. Откройте PuTTY
4. В поле **"Host Name"** введите: `root@ваш-ip-адрес`
5. Порт: `22`
6. Нажмите **"Open"**
7. Введите пароль root

### Шаг 7: Подключение через Mac

**Если у вас Mac:**

1. Откройте **"Терминал"** (найдите через Spotlight: Cmd+Space, введите "Терминал")

2. Введите команду (замените IP на ваш):
   ```bash
   ssh root@185.123.45.67
   ```

3. Нажмите Enter

4. Введите `yes` при первом подключении

5. Введите пароль root

6. Готово! Вы подключены!

---

## Часть 4: Первые команды - что это такое

### Что такое команды

**Команды** - это текстовые инструкции для компьютера. Вы вводите команду, нажимаете Enter, и компьютер выполняет действие.

**Важно:**
- Команды нужно вводить точно, как написано
- Регистр букв важен (большие и маленькие буквы - это разное)
- После каждой команды нажимайте Enter

### Шаг 8: Обновление системы

**Что мы делаем:** Обновляем список программ и устанавливаем обновления безопасности.

**Выполните команды по очереди** (копируйте и вставляйте в терминал):

```bash
apt update
```

Нажмите Enter. Вы увидите много текста - это нормально. Дождитесь завершения (когда появится `root@consultations-server:~#`).

```bash
apt upgrade -y
```

Нажмите Enter. Это займет 2-5 минут. Дождитесь завершения.

**Что происходит:** Система обновляется. Это важно для безопасности.

---

## Часть 5: Установка необходимых программ

### Шаг 9: Установка Python и других программ

**Что мы делаем:** Устанавливаем Python (язык программирования для вашего сайта) и другие нужные программы.

**Выполните команду:**

```bash
apt install -y python3 python3-pip python3-venv nginx supervisor git
```

Нажмите Enter. Это займет 3-5 минут.

**Что устанавливается:**
- `python3` - язык программирования
- `python3-pip` - установщик пакетов для Python
- `python3-venv` - создание изолированных окружений
- `nginx` - веб-сервер (показывает сайт в браузере)
- `supervisor` - программа для автозапуска вашего сайта
- `git` - система контроля версий (может понадобиться)

### Шаг 10: Проверка установки Python

**Выполните команду:**

```bash
python3 --version
```

Нажмите Enter. Вы должны увидеть что-то вроде:
```
Python 3.10.12
```

Если видите версию Python - все хорошо! ✅

---

## Часть 6: Создание папки для сайта

### Шаг 11: Создание директории

**Что мы делаем:** Создаем папку, где будут лежать файлы вашего сайта.

**Выполните команды:**

```bash
mkdir -p /var/www/consultations
```

Нажмите Enter.

```bash
cd /var/www/consultations
```

Нажмите Enter. Теперь вы находитесь в папке проекта.

**Объяснение команд:**
- `mkdir -p` - создать папку
- `/var/www/consultations` - путь к папке
- `cd` - перейти в папку

---

## Часть 7: Загрузка файлов проекта на сервер

### Шаг 12: Подготовка файлов на вашем компьютере

**Что нужно сделать:** Загрузить все файлы проекта на сервер.

**Вариант A: Через WinSCP (Windows) - РЕКОМЕНДУЕТСЯ**

1. **Скачайте WinSCP:**
   - Откройте: https://winscp.net/eng/download.php
   - Скачайте установщик
   - Установите программу

2. **Откройте WinSCP**

3. **Подключитесь к серверу:**
   - **File protocol:** SFTP
   - **Host name:** ваш IP-адрес (например: `185.123.45.67`)
   - **User name:** `root`
   - **Password:** ваш пароль root
   - Нажмите **"Login"**

4. **Загрузите файлы:**
   - Слева (ваш компьютер): найдите папку `/Users/tagirminnakhmetov/НК`
   - Справа (сервер): перейдите в `/var/www/consultations`
   - Выделите все файлы и папки слева:
     - `app/` (папка)
     - `static/` (папка)
     - `requirements.txt` (файл)
     - `README.md` (файл)
     - и другие файлы
   - Перетащите их вправо (на сервер)
   - Дождитесь завершения загрузки

**Важно:** НЕ загружайте папку `.venv` и файл базы данных `data/consultations.db` (если есть)

**Вариант B: Через командную строку (Mac/Linux)**

На вашем компьютере откройте терминал и выполните:

```bash
cd /Users/tagirminnakhmetov/НК
scp -r app static requirements.txt *.md root@ваш-ip-адрес:/var/www/consultations/
```

Замените `ваш-ip-адрес` на реальный IP.

### Шаг 13: Проверка загрузки файлов

**На сервере выполните:**

```bash
ls -la
```

Нажмите Enter. Вы должны увидеть список файлов:
```
app/
static/
requirements.txt
README.md
...
```

Если видите файлы - все хорошо! ✅

---

## Часть 8: Настройка Python окружения

### Шаг 14: Создание виртуального окружения

**Что мы делаем:** Создаем изолированное окружение для Python, чтобы не было конфликтов с другими программами.

**Выполните команды:**

```bash
python3 -m venv .venv
```

Нажмите Enter. Подождите 10-20 секунд.

```bash
source .venv/bin/activate
```

Нажмите Enter. Обратите внимание - в начале строки появилось `(.venv)`:
```
(.venv) root@consultations-server:/var/www/consultations#
```

Это означает, что виртуальное окружение активировано! ✅

### Шаг 15: Установка зависимостей

**Что мы делаем:** Устанавливаем все библиотеки, которые нужны вашему сайту.

**Выполните команды:**

```bash
pip install --upgrade pip
```

Нажмите Enter. Подождите 10-20 секунд.

```bash
pip install -r requirements.txt
```

Нажмите Enter. **Это займет 5-10 минут!** Вы увидите много текста - это нормально. Дождитесь завершения.

**Что происходит:** Устанавливаются все необходимые библиотеки для работы сайта.

**Важно:** Не закрывайте терминал во время установки!

---

## Часть 9: Настройка переменных окружения

### Шаг 16: Создание файла .env

**Что мы делаем:** Создаем файл с паролями и настройками.

**Выполните команду:**

```bash
nano .env
```

Нажмите Enter. Откроется текстовый редактор.

### Шаг 17: Заполнение файла .env

**В открывшемся редакторе вставьте следующий текст:**

```env
DATABASE_URL=sqlite:///./data/consultations.db
ADMIN_TOKEN=ваш-сложный-пароль-админа-12345
EXPERT_TOKEN=ваш-сложный-пароль-эксперта-67890
CORS_ORIGINS=*
```

**ВАЖНО:** Замените пароли на свои:
- `ваш-сложный-пароль-админа-12345` → придумайте свой пароль для админа
- `ваш-сложный-пароль-эксперта-67890` → придумайте свой пароль для эксперта

**Пример хороших паролей:**
- `Admin2024!SecretPass`
- `Expert2024!SecretPass`

**Как вставить текст:**
- Нажмите правой кнопкой мыши в окне терминала
- Или используйте комбинацию клавиш для вставки (обычно Shift+Insert)

### Шаг 18: Сохранение файла

**После вставки текста:**

1. Нажмите **Ctrl + X** (выход)
2. Нажмите **Y** (да, сохранить)
3. Нажмите **Enter** (подтвердить имя файла)

Файл сохранен! ✅

---

## Часть 10: Создание папки для базы данных

### Шаг 19: Создание директории data

**Выполните команды:**

```bash
mkdir -p data
```

Нажмите Enter.

```bash
chmod 755 data
```

Нажмите Enter.

**Что мы сделали:** Создали папку для базы данных и дали ей нужные права доступа.

---

## Часть 11: Настройка автозапуска сайта

### Шаг 20: Создание файла сервиса

**Что мы делаем:** Создаем файл, который будет автоматически запускать ваш сайт при перезагрузке сервера.

**Выполните команду:**

```bash
nano /etc/systemd/system/consultations.service
```

Нажмите Enter. Откроется редактор.

### Шаг 21: Заполнение файла сервиса

**Вставьте следующий текст (скопируйте полностью):**

```ini
[Unit]
Description=Consultation Booking Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/var/www/consultations
Environment="PATH=/var/www/consultations/.venv/bin"
ExecStart=/var/www/consultations/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**Вставьте текст** (правой кнопкой мыши или Shift+Insert)

**Сохраните:**
1. **Ctrl + X**
2. **Y**
3. **Enter**

### Шаг 22: Активация сервиса

**Выполните команды по очереди:**

```bash
systemctl daemon-reload
```

Нажмите Enter.

```bash
systemctl enable consultations
```

Нажмите Enter. Вы увидите:
```
Created symlink /etc/systemd/system/multi-user.target.wants/consultations.service
```

Это хорошо! ✅

```bash
systemctl start consultations
```

Нажмите Enter.

```bash
systemctl status consultations
```

Нажмите Enter. Вы должны увидеть:
```
● consultations.service - Consultation Booking Service
     Loaded: loaded (/etc/systemd/system/consultations.service; enabled)
     Active: active (running) since ...
```

**Важно:** Должно быть написано **"active (running)"** зеленым цветом!

Если видите ошибку, выполните:
```bash
journalctl -u consultations -n 50
```

Это покажет последние 50 строк логов. Скопируйте ошибку и проверьте, все ли файлы загружены правильно.

---

## Часть 12: Настройка веб-сервера Nginx

### Шаг 23: Создание конфигурации Nginx

**Что мы делаем:** Настраиваем Nginx, чтобы он показывал ваш сайт в браузере.

**Выполните команду:**

```bash
nano /etc/nginx/sites-available/consultations
```

Нажмите Enter.

### Шаг 24: Заполнение конфигурации Nginx

**Вставьте следующий текст** (замените `ваш-ip-адрес` на реальный IP вашего сервера):

```nginx
server {
    listen 80;
    server_name ваш-ip-адрес;

    # Логи
    access_log /var/log/nginx/consultations_access.log;
    error_log /var/log/nginx/consultations_error.log;

    # Максимальный размер загружаемых файлов
    client_max_body_size 10M;

    # Проксирование на FastAPI
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }

    # Кэширование статических файлов
    location /static/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_cache_valid 200 1h;
        add_header Cache-Control "public, immutable";
    }
}
```

**ВАЖНО:** Замените `ваш-ip-адрес` на реальный IP (например: `185.123.45.67`)

**Сохраните:**
1. **Ctrl + X**
2. **Y**
3. **Enter**

### Шаг 25: Активация конфигурации

**Выполните команды:**

```bash
ln -s /etc/nginx/sites-available/consultations /etc/nginx/sites-enabled/
```

Нажмите Enter.

```bash
rm -f /etc/nginx/sites-enabled/default
```

Нажмите Enter (удаляем дефолтную конфигурацию).

```bash
nginx -t
```

Нажмите Enter. Вы должны увидеть:
```
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

Если видите это - все хорошо! ✅

### Шаг 26: Запуск Nginx

```bash
systemctl reload nginx
```

Нажмите Enter.

```bash
systemctl status nginx
```

Нажмите Enter. Должно быть **"active (running)"**.

---

## Часть 13: Настройка файрвола

### Шаг 27: Открытие портов

**Что мы делаем:** Разрешаем доступ к сайту из интернета.

**Выполните команды:**

```bash
ufw allow 22/tcp
```

Нажмите Enter (разрешаем SSH - важно сделать первым!).

```bash
ufw allow 80/tcp
```

Нажмите Enter (разрешаем HTTP).

```bash
ufw allow 443/tcp
```

Нажмите Enter (разрешаем HTTPS).

```bash
ufw enable
```

Нажмите Enter. На вопрос ответьте **`y`** и нажмите Enter.

```bash
ufw status
```

Нажмите Enter. Вы должны увидеть список разрешенных портов.

---

## Часть 14: Проверка работы сайта

### Шаг 28: Тестирование

**Откройте браузер на вашем компьютере** и введите в адресной строке:

```
http://ваш-ip-адрес
```

Замените `ваш-ip-адрес` на реальный IP сервера.

**Вы должны увидеть главную страницу сайта!** 🎉

**Проверьте все страницы:**
- `http://ваш-ip-адрес/` - главная
- `http://ваш-ip-адрес/student` - страница студентов
- `http://ваш-ip-адрес/expert/login` - вход эксперта (пароль из .env файла)
- `http://ваш-ip-адрес/admin/login` - вход администратора (пароль из .env файла)

---

## Часть 15: Полезные команды (на будущее)

### Просмотр логов

**Если что-то не работает, посмотрите логи:**

```bash
journalctl -u consultations -n 50
```

Показывает последние 50 строк логов приложения.

```bash
tail -f /var/log/nginx/consultations_error.log
```

Показывает ошибки Nginx (для выхода нажмите Ctrl+C).

### Перезапуск сайта

**Если вы обновили файлы:**

```bash
systemctl restart consultations
```

### Остановка сайта

```bash
systemctl stop consultations
```

### Запуск сайта

```bash
systemctl start consultations
```

### Проверка статуса

```bash
systemctl status consultations
```

---

## Решение проблем

### Проблема: Сайт не открывается

**Решение:**

1. Проверьте, что приложение запущено:
   ```bash
   systemctl status consultations
   ```
   Должно быть "active (running)"

2. Проверьте, что Nginx работает:
   ```bash
   systemctl status nginx
   ```

3. Проверьте логи:
   ```bash
   journalctl -u consultations -n 50
   ```

### Проблема: Ошибка 502 Bad Gateway

**Решение:**

Это означает, что Nginx не может подключиться к приложению.

1. Проверьте, что приложение запущено:
   ```bash
   systemctl status consultations
   ```

2. Перезапустите приложение:
   ```bash
   systemctl restart consultations
   ```

3. Проверьте логи:
   ```bash
   journalctl -u consultations -n 50
   ```

### Проблема: "Permission denied" (отказано в доступе)

**Решение:**

```bash
chown -R root:root /var/www/consultations
chmod -R 755 /var/www/consultations
```

### Проблема: Не могу подключиться по SSH

**Решение:**

1. Проверьте IP-адрес в панели Timeweb
2. Убедитесь, что используете правильный пароль
3. Попробуйте подключиться через веб-консоль в панели Timeweb

---

## Что делать дальше

1. **Сохраните пароли:**
   - Пароль root от сервера
   - Пароль админа (из файла .env)
   - Пароль эксперта (из файла .env)
   - IP-адрес сервера

2. **Настройте домен (опционально):**
   - Если у вас есть домен, настройте его на этот IP
   - Инструкция по настройке домена есть в `DEPLOY.md`

3. **Настройте SSL (HTTPS):**
   - Для безопасности лучше использовать HTTPS
   - Инструкция есть в `DEPLOY.md`

---

## Поздравляю! 🎉

Ваш сайт должен быть доступен по адресу `http://ваш-ip-адрес`!

Если что-то не работает - проверьте раздел "Решение проблем" выше или обратитесь в поддержку Timeweb.

---

## Контакты поддержки Timeweb

- **Телефон:** 8 800 100-24-15 (бесплатно по России)
- **Email:** support@timeweb.com
- **Чат:** в панели управления Timeweb

---

**Удачи с вашим сайтом!** 🚀

