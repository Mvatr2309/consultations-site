# Подробная инструкция по деплою на Yandex Cloud

Эта инструкция рассчитана на новичков и содержит все шаги от создания аккаунта до запуска сайта.

## Часть 1: Регистрация и создание сервера

### Шаг 1: Регистрация в Yandex Cloud

1. Откройте сайт: https://cloud.yandex.ru/
2. Нажмите кнопку **"Начать работу"** или **"Войти"**
3. Войдите через Яндекс ID (если нет аккаунта - создайте)
4. При первом входе нужно будет:
   - Принять условия использования
   - Указать способ оплаты (карта)
   - **Важно:** Первые 3000 рублей даются бесплатно на тестирование!

### Шаг 2: Создание облака (Cloud)

1. После входа вы увидите панель управления
2. Нажмите на **"Создать облако"** или выберите существующее
3. Укажите название, например: "Мой сайт консультаций"
4. Нажмите **"Создать"**

### Шаг 3: Создание виртуальной машины (VM)

1. В меню слева найдите **"Compute Cloud"** → **"Виртуальные машины"**
2. Нажмите кнопку **"Создать виртуальную машину"**

#### Настройка виртуальной машины:

**3.1. Основные параметры:**
- **Имя:** `consultations-server` (или любое другое)
- **Зона доступности:** выберите ближайшую к вам (например, `ru-central1-a`)

**3.2. Образы:**
- Нажмите **"Выбрать"** в разделе "Образы"
- Выберите **"Ubuntu"** (рекомендуется Ubuntu 22.04 LTS)
- Нажмите **"Создать ВМ"**

**3.3. Вычислительные ресурсы:**
- **Платформа:** Intel Ice Lake
- **vCPU:** 2 ядра (достаточно для начала)
- **RAM:** 4 ГБ (достаточно для начала)
- **Диск:** 20 ГБ SSD (можно увеличить позже)

**3.4. Сетевые настройки:**
- **Подсеть:** оставьте по умолчанию или создайте новую
- **Публичный адрес:** выберите **"Автоматически"** (это даст вам IP-адрес для доступа)

**3.5. Доступ:**
- **Логин:** `ubuntu` (по умолчанию)
- **SSH-ключ:** 
  - Если у вас уже есть SSH-ключ, добавьте его
  - Если нет, создайте новый:
    - Нажмите **"Создать новый ключ"**
    - Скачайте файл ключа (он понадобится для подключения)
    - Сохраните его в безопасном месте!

**3.6. Проверьте все настройки и нажмите "Создать ВМ"**

### Шаг 4: Ожидание создания сервера

- Создание займет 2-5 минут
- Дождитесь статуса **"Running"** (работает)
- Запишите **публичный IP-адрес** (он будет показан в списке ВМ)

---

## Часть 2: Подключение к серверу

### Шаг 5: Подключение через SSH (Windows)

**Если у вас Windows:**

1. **Установите PuTTY:**
   - Скачайте с https://www.putty.org/
   - Установите программу

2. **Подключение через PuTTY:**
   - Откройте PuTTY
   - В поле **"Host Name"** введите: `ubuntu@ваш-ip-адрес`
   - Порт: `22`
   - В разделе **"Connection" → "SSH" → "Auth"**:
     - Нажмите **"Browse"**
     - Выберите скачанный файл ключа (файл `.pem` или `.ppk`)
   - Нажмите **"Open"**
   - При первом подключении нажмите **"Yes"** на предупреждение

**Если у вас Mac или Linux:**

1. Откройте **Терминал**
2. Перейдите в папку, где сохранен ключ
3. Выполните команду:
   ```bash
   chmod 400 ваш-ключ.pem
   ssh -i ваш-ключ.pem ubuntu@ваш-ip-адрес
   ```

### Шаг 6: Первый вход на сервер

После подключения вы увидите приглашение командной строки:
```
ubuntu@consultations-server:~$
```

Поздравляю! Вы подключились к серверу! 🎉

---

## Часть 3: Установка необходимого ПО

### Шаг 7: Обновление системы

Выполните команды по очереди (копируйте и вставляйте в терминал):

```bash
# Обновление списка пакетов
sudo apt update

# Обновление системы
sudo apt upgrade -y
```

Это займет несколько минут. Дождитесь завершения.

### Шаг 8: Установка Python и необходимых программ

```bash
# Установка Python и инструментов
sudo apt install -y python3 python3-pip python3-venv nginx supervisor git

# Проверка версии Python (должна быть 3.10 или выше)
python3 --version
```

### Шаг 9: Создание директории для проекта

```bash
# Создаем папку для сайта
sudo mkdir -p /var/www/consultations
sudo chown -R ubuntu:ubuntu /var/www/consultations
cd /var/www/consultations
```

---

## Часть 4: Загрузка файлов проекта на сервер

### Вариант A: Через Git (если проект в репозитории)

```bash
# Клонируем проект
git clone https://ваш-репозиторий.git .

# Или если репозиторий приватный, используйте SSH-ключ
```

### Вариант B: Через SCP (загрузка файлов с компьютера)

**На вашем компьютере (в новом окне терминала):**

```bash
# Перейдите в папку с проектом
cd /Users/tagirminnakhmetov/НК

# Загрузите файлы на сервер
scp -i путь-к-ключу.pem -r * ubuntu@ваш-ip-адрес:/var/www/consultations/
```

**Или используйте WinSCP (для Windows):**
1. Скачайте WinSCP: https://winscp.net/
2. Подключитесь к серверу (используя IP и ключ)
3. Перетащите файлы из папки проекта в `/var/www/consultations/`

### Вариант C: Через встроенный редактор Yandex Cloud

1. В панели Yandex Cloud откройте вашу ВМ
2. Нажмите **"Подключиться"** → **"Веб-терминал"**
3. Используйте встроенный редактор для создания файлов

**После загрузки файлов проверьте:**

```bash
cd /var/www/consultations
ls -la
```

Вы должны увидеть файлы: `app/`, `static/`, `requirements.txt`, и т.д.

---

## Часть 5: Настройка проекта

### Шаг 10: Создание виртуального окружения

```bash
cd /var/www/consultations

# Создаем виртуальное окружение
python3 -m venv .venv

# Активируем его
source .venv/bin/activate

# Обновляем pip
pip install --upgrade pip

# Устанавливаем зависимости
pip install -r requirements.txt
```

Это займет 5-10 минут. Дождитесь завершения.

### Шаг 11: Создание файла с настройками

```bash
# Создаем файл .env
nano .env
```

В открывшемся редакторе вставьте:

```env
DATABASE_URL=sqlite:///./data/consultations.db
ADMIN_TOKEN=ваш-сложный-пароль-админа-12345
EXPERT_TOKEN=ваш-сложный-пароль-эксперта-67890
CORS_ORIGINS=*
```

**Важно:** Замените пароли на свои сложные пароли!

**Как выйти из nano:**
- Нажмите `Ctrl + X`
- Нажмите `Y` (да, сохранить)
- Нажмите `Enter`

### Шаг 12: Создание папки для базы данных

```bash
mkdir -p data
chmod 755 data
```

---

## Часть 6: Настройка автозапуска (systemd)

### Шаг 13: Создание сервиса

```bash
# Создаем файл сервиса
sudo nano /etc/systemd/system/consultations.service
```

Вставьте следующее:

```ini
[Unit]
Description=Consultation Booking Service
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/var/www/consultations
Environment="PATH=/var/www/consultations/.venv/bin"
ExecStart=/var/www/consultations/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Сохраните: `Ctrl + X`, затем `Y`, затем `Enter`

### Шаг 14: Запуск сервиса

```bash
# Перезагружаем systemd
sudo systemctl daemon-reload

# Включаем автозапуск
sudo systemctl enable consultations

# Запускаем сервис
sudo systemctl start consultations

# Проверяем статус
sudo systemctl status consultations
```

Если все хорошо, вы увидите **"active (running)"** зеленым цветом.

**Просмотр логов:**
```bash
sudo journalctl -u consultations -f
```

---

## Часть 7: Настройка Nginx (веб-сервер)

### Шаг 15: Создание конфигурации Nginx

```bash
# Создаем файл конфигурации
sudo nano /etc/nginx/sites-available/consultations
```

Вставьте следующее (замените `ваш-домен.ru` на ваш домен или IP):

```nginx
server {
    listen 80;
    server_name ваш-ip-адрес или ваш-домен.ru;

    # Логи
    access_log /var/log/nginx/consultations_access.log;
    error_log /var/log/nginx/consultations_error.log;

    # Максимальный размер загружаемых файлов
    client_max_body_size 10M;

    # Проксирование на FastAPI
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_redirect off;
    }

    # Кэширование статических файлов
    location /static/ {
        proxy_pass http://127.0.0.1:8000;
        proxy_cache_valid 200 1h;
        add_header Cache-Control "public, immutable";
    }
}
```

Сохраните файл.

### Шаг 16: Активация конфигурации

```bash
# Создаем символическую ссылку
sudo ln -s /etc/nginx/sites-available/consultations /etc/nginx/sites-enabled/

# Удаляем дефолтную конфигурацию (если есть)
sudo rm -f /etc/nginx/sites-enabled/default

# Проверяем конфигурацию
sudo nginx -t
```

Если все правильно, вы увидите: **"syntax is ok"** и **"test is successful"**

### Шаг 17: Запуск Nginx

```bash
# Перезагружаем Nginx
sudo systemctl reload nginx

# Проверяем статус
sudo systemctl status nginx
```

---

## Часть 8: Настройка файрвола

### Шаг 18: Открытие портов

```bash
# Устанавливаем ufw (если еще не установлен)
sudo apt install -y ufw

# Разрешаем SSH (важно сделать первым!)
sudo ufw allow 22/tcp

# Разрешаем HTTP
sudo ufw allow 80/tcp

# Разрешаем HTTPS
sudo ufw allow 443/tcp

# Включаем файрвол
sudo ufw enable

# Проверяем статус
sudo ufw status
```

---

## Часть 9: Проверка работы

### Шаг 19: Тестирование сайта

1. Откройте браузер
2. Введите в адресной строке: `http://ваш-ip-адрес`
3. Вы должны увидеть главную страницу сайта!

**Проверьте все страницы:**
- `http://ваш-ip-адрес/` - главная
- `http://ваш-ip-адрес/student` - страница студентов
- `http://ваш-ip-адрес/expert/login` - вход эксперта
- `http://ваш-ip-адрес/admin/login` - вход администратора

---

## Часть 10: Настройка домена (опционально)

### Шаг 20: Подключение домена

Если у вас есть домен:

1. В панели управления вашим доменом найдите настройки DNS
2. Добавьте A-запись:
   - **Имя:** `@` (или оставьте пустым)
   - **Тип:** `A`
   - **Значение:** ваш IP-адрес сервера
   - **TTL:** `3600`

3. Подождите 10-30 минут для распространения DNS

4. Обновите конфигурацию Nginx (замените IP на домен):
   ```bash
   sudo nano /etc/nginx/sites-available/consultations
   ```
   Измените `server_name` на ваш домен

5. Перезагрузите Nginx:
   ```bash
   sudo nginx -t
   sudo systemctl reload nginx
   ```

### Шаг 21: Настройка SSL (HTTPS) через Let's Encrypt

```bash
# Установка certbot
sudo apt install -y certbot python3-certbot-nginx

# Получение SSL сертификата
sudo certbot --nginx -d ваш-домен.ru -d www.ваш-домен.ru

# Следуйте инструкциям на экране
# Введите email для уведомлений
# Согласитесь с условиями
```

После этого сайт будет доступен по HTTPS!

---

## Часть 11: Полезные команды

### Управление сервисом

```bash
# Перезапуск приложения
sudo systemctl restart consultations

# Остановка приложения
sudo systemctl stop consultations

# Запуск приложения
sudo systemctl start consultations

# Просмотр логов
sudo journalctl -u consultations -n 50
```

### Обновление кода

```bash
cd /var/www/consultations

# Если используете git:
git pull

# Обновляем зависимости (если requirements.txt изменился)
source .venv/bin/activate
pip install -r requirements.txt

# Перезапускаем сервис
sudo systemctl restart consultations
```

### Просмотр логов

```bash
# Логи приложения
sudo journalctl -u consultations -f

# Логи Nginx
sudo tail -f /var/log/nginx/consultations_error.log
```

---

## Часть 12: Резервное копирование

### Создание скрипта бэкапа

```bash
# Создаем скрипт
nano ~/backup-consultations.sh
```

Вставьте:

```bash
#!/bin/bash
BACKUP_DIR="/home/ubuntu/backups"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR
cp /var/www/consultations/data/consultations.db $BACKUP_DIR/consultations_$DATE.db
# Удаляем старые бэкапы (старше 30 дней)
find $BACKUP_DIR -name "consultations_*.db" -mtime +30 -delete
echo "Backup completed: consultations_$DATE.db"
```

```bash
# Делаем скрипт исполняемым
chmod +x ~/backup-consultations.sh

# Тестируем
~/backup-consultations.sh
```

### Настройка автоматического бэкапа

```bash
# Открываем планировщик задач
crontab -e

# Добавляем строку (бэкап каждый день в 2:00 ночи)
0 2 * * * /home/ubuntu/backup-consultations.sh
```

---

## Решение проблем

### Сайт не открывается

1. **Проверьте, что приложение запущено:**
   ```bash
   sudo systemctl status consultations
   ```

2. **Проверьте, что Nginx работает:**
   ```bash
   sudo systemctl status nginx
   ```

3. **Проверьте логи:**
   ```bash
   sudo journalctl -u consultations -n 50
   sudo tail -n 50 /var/log/nginx/consultations_error.log
   ```

4. **Проверьте файрвол:**
   ```bash
   sudo ufw status
   ```

### Ошибка 502 Bad Gateway

Это означает, что Nginx не может подключиться к приложению.

1. Проверьте, что приложение запущено на порту 8000:
   ```bash
   sudo netstat -tlnp | grep 8000
   ```

2. Перезапустите приложение:
   ```bash
   sudo systemctl restart consultations
   ```

### Проблемы с правами доступа

```bash
# Установите правильные права
sudo chown -R ubuntu:ubuntu /var/www/consultations
sudo chmod -R 755 /var/www/consultations
```

### Проблемы с базой данных

```bash
# Проверьте права на папку data
ls -la /var/www/consultations/data

# Если нужно, исправьте права
chmod 755 /var/www/consultations/data
```

---

## Безопасность

### Рекомендации:

1. **Используйте сложные пароли** для `ADMIN_TOKEN` и `EXPERT_TOKEN`
2. **Регулярно обновляйте систему:**
   ```bash
   sudo apt update && sudo apt upgrade -y
   ```
3. **Настройте автоматические бэкапы**
4. **Не делитесь SSH-ключом** ни с кем
5. **Используйте HTTPS** (SSL сертификат)

---

## Стоимость

Примерная стоимость на Yandex Cloud:
- **ВМ с 2 vCPU, 4 ГБ RAM, 20 ГБ SSD:** ~1000-1500 руб/месяц
- **Публичный IP:** обычно включен в тариф
- **Трафик:** первые 10 ГБ бесплатно, далее ~1-2 руб/ГБ

**Совет:** Используйте бесплатные 3000 рублей для тестирования!

---

## Поддержка

Если что-то не работает:
1. Проверьте логи (см. раздел "Полезные команды")
2. Обратитесь в поддержку Yandex Cloud: https://cloud.yandex.ru/support
3. Проверьте документацию: https://cloud.yandex.ru/docs/

---

**Поздравляю! Ваш сайт должен быть доступен по адресу `http://ваш-ip-адрес`!** 🎉

